- if logged_in?
  %p.nav.user
    = link_to "Welcome, #{current_user.name} Guru", root_path
  %p.nav.user
    = link_to 'Log Out', logout_path, method: :delete
- else
  %p.nav.user
    = link_to 'LogIn', login_path
    = link_to 'SignUp', signup_path

