- question = @test.questions.build
%h1= question_header(question)
= render 'form', test: @test, 
                 question: question, 
                 url: admin_test_questions_path(@test)
%br
= link_to 'Back', admin_test_path(@test)
