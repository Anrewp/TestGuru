- if user_signed_in?
  .nav.nav-admin
    %span.nav-admin.title TestGuru Admin Panel
    .nav-admin.user
      = "Welcome, #{current_user.name} Guru Admin | "
      = link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'nav-admin user-email'
